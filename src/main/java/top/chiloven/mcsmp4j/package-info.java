/**
 * Public entry points for mcsmp4j.
 *
 * <p>Create a client with {@link top.chiloven.mcsmp4j.McsmpClient#builder()}, connect it to a
 * Minecraft dedicated server management endpoint, then use the strongly typed API groups or the raw
 * JSON-RPC API for extension namespaces.</p>
 */
@NullMarked
package top.chiloven.mcsmp4j;

import org.jspecify.annotations.NullMarked;
