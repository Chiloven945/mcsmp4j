/**
 * Low-level JSON-RPC notification types exposed by mcsmp4j.
 *
 * <p>Most applications should prefer the typed event API in {@link top.chiloven.mcsmp4j.event}. This package is
 * useful for custom namespaces, diagnostics, and integrations that need direct access to raw JSON-RPC
 * notifications.</p>
 */
@NullMarked
package top.chiloven.mcsmp4j.protocol;

import org.jspecify.annotations.NullMarked;
